{
  "name": "sights",
  "type": "module",
  "scripts": {
    "generate:image-list": "node scripts/generate-image-list.js",
    "build": "npm run generate:image-list && npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.21.0"
  }
}

